{
  "name": "meet-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "build:ts": "tsc",
    "build:css": "tailwindcss -i ./src/styles.css -o ./dist/styles.css",
    "build:copy": "cp src/manifest.json dist/ && cp src/popup.html dist/",
    "build": "npm run build:ts && npm run build:css && npm run build:copy",
    "watch:ts": "tsc -w",
    "watch:css": "tailwindcss -i ./src/styles.css -o ./dist/styles.css --watch",
    "dev": "npm run build && concurrently \"npm run watch:ts\" \"npm run watch:css\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "^0.0.263",
    "concurrently": "^8.2.2",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.4.3"
  }
}
